export default function Header(props) {
  //{title : "REACT" onChangeMode : (0=>{alert("Header")})}
  return (
    <header>
      <h1>
        <a
          href="/"
          onClick={(e) => {
            e.preventDefault();
            props.onChangeMode();
          }}
        >
          {props.title}
        </a>
      </h1>
    </header>
  );
}
